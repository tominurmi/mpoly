{
  "name": "mpoly",
  "version": "1.0.0",
  "description": "Interactive forms. This is a small part of a larger project",
  "author": "Tomi",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate",
    "pushit": "nuxt generate && surge ./dist https://mpoly.surge.sh"
  },
  "dependencies": {
    "@nuxtjs/google-tag-manager": "^2.1.4",
    "cross-env": "^5.2.0",
    "hapi-nuxt": "^1.0.1",
    "node-sass": "^4.11.0",
    "nuxt": "^2.4.0",
    "sass-loader": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
